@model IEnumerable<CRUD.Models.TruckRefBrands>

@{
    ViewBag.Title = "Truck Brands";
}

<h2>Truck Brands</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TruckRefBrandName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TruckRefBrandName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TruckRefBrandID }) |
            @Html.ActionLink("Details", "Details", new { id=item.TruckRefBrandID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TruckRefBrandID })
        </td>
    </tr>
}

</table>
